{
  "name": "intern-attendance",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Intern Attendance Fullstack System (Ionic React + NestJS Fastify Node)",
  "scripts": {
    "setup": "npm install --prefix server && npm install --prefix ClientApp",
    "dev:client": "npm run dev --prefix ClientApp",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix ClientApp && npm run build --prefix server && node scripts/copy-client.mjs",
    "start": "node server/dist/main.js",
    "package": "node scripts/package-aapanel.mjs",
    "ship": "node scripts/publish-production-branch.mjs && git push origin production",
    "save": "node scripts/git-push.mjs",
    "migrate": "npm --prefix server run migrate",
    "migrate:status": "npm --prefix server run migrate:status",
    "seed:superadmin": "npm --prefix server run seed:superadmin",
    "clean": "node -e \"import('node:fs').then(fs => { for (const p of ['public', 'server/public', 'server/dist', 'ClientApp/dist', 'ClientApp/dev-dist']) { fs.rmSync(p, { recursive: true, force: true }); } })\""
  },
  "engines": {
    "node": ">=20"
  }
}
